{
    "builds": [{
        "src": "storefront-api/wsgi.py",
        "use": "@ardnt/vercel-python.wsgi",
        "config":{"maxLambdaSize":"15mb"}
    }],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"storefront-api/wsgi.py"
        }
    ]
}